# Personal data — these contain absolute paths into your own media library,
# including filenames. Never commit them.
favorites.json
favorites.json.backup

# Build output. The app is ~60MB and the DMG ~25MB; ship the DMG as a
# GitHub Release asset instead of committing it.
FolderVideoPlayer.app/
FolderVideoPlayer.dmg
build/
dist/
venv/
__pycache__/
*.pyc

# macOS / editor cruft
.DS_Store
.claude/
build.log

# VLCKit, fetched rather than committed — 87MB of third-party binary
vendor/
